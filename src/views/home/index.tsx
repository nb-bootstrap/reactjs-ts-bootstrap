import { ReactElement } from 'react';
import Header from './components/Header';
const Home = (): ReactElement => {
    return (
        <>
            <Header />
            <p>IM Home</p>
        </>
    );
};

export default Home;
